<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta http-equiv="X-UA-Compatible" content="IE=edge">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <link rel="stylesheet" href="/styles.css">
    <title>Document</title>
</head>
<body>
    <header>
        <div class="header">
            <a href="/">
            <h1>RUN<span>NER</span></h1>
            </a>
            <a class="add-btn" href="#">ADD <span>+</span></a>
        </div>
    </header>
    <main>
        <div class="shoes">
        <% if(shoes.length > 0) { %> <% shoes.forEach(shoe => { %>
            <div class="shoes-container" style="background-image: url('<%= shoe.image %>')">
                <div class="shoes-box-top">
                    <p class="shoes-brand"><%= shoe.brand %></p>
                    <h3 class="shoes-title"><%= shoe.title %></h3>
                </div>
                <div class="shoes-box-bottom">
                    <p class="shoes-price-text">Price</p>
                    <p class="shoes-price"><%= shoe.price %>:-</p>
                </div>
                <div class="shoes-btn">
                    <a href="/shoes/<%= shoe.id %>">Details</a>
                </div>
            </div>
        <% }) %> <% } else { %>
        <p>No shoes to display...</p>
        <% } %>
        </div>
      </main>
</body>
</html>